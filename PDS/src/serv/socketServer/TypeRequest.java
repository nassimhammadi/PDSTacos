package serv.socketServer;

public enum TypeRequest {
	 UPDATE,
	 INSERT,
	 DELETE,
	 SELECT,
	 LOGIN,
	 SELECTID,
	 SELECTB,
	 IND_SELECTNBREP,
	 FINISH,
	 FINISHB,
	 OCCURED,
	 FINISHED,
	 SELECT_MONITORER
}
